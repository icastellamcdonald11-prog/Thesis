# Non-secret tunables. Secrets (API keys, SMTP credentials) come from the
# environment / GitHub Actions secrets — see .env.example.

timezone: "Asia/Shanghai"

acquisition:
  # rsshub.app blocks datacenter IPs (confirmed: instant 403 on every route from
  # GitHub Actions runners, 2026-07-05). Fallback mirrors are tried in order after
  # the primary fails. Public mirrors come and go — the durable fix is self-hosting
  # RSSHub (one Docker container) and putting its URL first here.
  rsshub_base_url: "https://rsshub.app"
  rsshub_fallback_urls:
    - "https://rsshub.rssforever.com"
    - "https://hub.slarker.me"
    - "https://rsshub.pseudoyu.com"
  user_agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"
  request_timeout_seconds: 15
  max_retries: 3
  backoff_base_seconds: 2
  per_domain_min_interval_seconds: 3
  fuzzy_title_similarity_threshold: 0.85
  fuzzy_dedupe_window_hours: 72

triage:
  model: "claude-haiku-4-5-20251001"
  batch_size: 25
  max_tokens: 4096
  score_threshold_total: 4
  score_threshold_in_niche: 1

cluster:
  window_days: 7
  min_sources: 3

# Stage 3 — coverage scan: web search + headline translation only, no analysis.
# Haiku instead of Sonnet (~3x cheaper per token) and 3 searches per item
# instead of 6 (web_search bills ~$0.01/search on top of tokens). Worst case at
# the default cap: 15 items x 3 searches = $0.45 in searches plus a few cents
# of Haiku tokens — comfortably under the $1/day target.
coverage:
  model: "claude-haiku-4-5"
  daily_cap: 15
  # 1024 truncated the JSON mid-object on 5/15 items in the first live run
  # (stories with lots of coverage hits) — output is still cheap at Haiku rates.
  max_tokens: 2048
  max_searches: 3

digest:
  output_dir: "digests"
  min_candidates: 5
  max_candidates: 10
  # Trailing window for the "New from ministries & think tanks" section.
  official_window_hours: 24

email:
  smtp_host: "smtp.gmail.com"
  smtp_port: 587
  use_tls: true
  subject_prefix: "[FT China Pitch Digest]"

storage:
  db_path: "data/pitch_discovery.db"
  feedback_csv: "feedback.csv"
